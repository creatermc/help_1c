

ТаблицаДанных = яя_СпециальногоНазначенияВызовСервера.ПолучитьГлобальноеЗначениеСериализованное(ИмяПеременной);	


// Возвращает глобальное значение сериализации по имени ИмяГлобальногоЗначения: ресурс СериализованноеЗначение из регистра сведений ГлобальныеЗначения по измерению Параметр.
Функция ПолучитьГлобальноеЗначениеСериализованное(ИмяГлобальногоЗначения) Экспорт
	
	Значение = Неопределено;
	
	СтруктураОтбор = Новый Структура;
	СтруктураОтбор.Вставить("Параметр", ИмяГлобальногоЗначения);
	СериализованноеЗначение = РегистрыСведений.яя_ГлобальныеЗначения.Получить(СтруктураОтбор).СериализованноеЗначение;
	
	Если ЗначениеЗаполнено(СериализованноеЗначение) Тогда
		Значение = ОбщегоНазначения.ЗначениеИзСтрокиXML(СериализованноеЗначение);
	КонецЕсли;
	
	Возврат Значение;
	
КонецФункции





Форма списка, отбор по родителю
    ФормаВыбора = Справочники.ОбъектыОИС.ПолучитьФормуВыбора("ФормаСписка");    
    ФормаВыбора.Отбор.Наименование.ВидСравнения = ВидСравнения.Равно;
    ФормаВыбора.Отбор.Наименование.Значение = "Информационные системы";
    ФормаВыбора.Отбор.Наименование.Использование = Истина;
    ФормаВыбора.Открыть();


У ФормыВыбора Родитель есть только в
ФормаВыбора.Отбор.Ссылка.Значение.Родитель.Наименование

Но тогда ругается на:
Значение не является значением объектного типа (ВидСравнения)
ФормаВыбора.Отбор.Ссылка.Значение.Родитель.Наименование.ВидСравнения = ВидСравнения.Равно;
Если смотреть через "вычислить выражение" то ВидСравнения есть только в ФормаВыбора.Отбор.Ссылка.ВидСравнения


(6) Так не работает что-ли?
&НаКлиенте
Процедура ОбъектОИСНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
    
    СтандартнаяОбработка = Ложь;
    
    УсловияОтбора = Новый Структура;
    УсловияОтбора.Вставить("Родитель", ПолучитьРодителяНаСервере());
    ПараметрыФормы = Новый Структура("Отбор", УсловияОтбора);
    ОписаниеЗавершения = Новый ОписаниеОповещения("ОбъектОИСНачалоВыбораЗавершение", ЭтаФорма);
    ОткрытьФорму("Справочник.ОбъектыОИС.ФормаВыбора", ПараметрыФормы, ЭтаФорма,,,,ОписаниеЗавершения);
    
КонецПроцедуры



9.  spacecraft 28.04.18 14:12
(1) (5) может так?
ФормаВыбора.Отбор.Наименование.ВидСравнения = ВидСравнения.ВИерархии ;

PS. Тут только не на Наименование отбор нужно, а на ссылку. И в значение подставлять ссылку.
   
20.  user876792 28.04.18 15:26
(9)
И в значение подставлять ссылку.

Непонятно что и куда подставлять
ФормаВыбора.Отбор.Ссылка.Значение = сюда? Ссылку на родителя элемента справочника?
   
21.  spacecraft 28.04.18 15:27 +0.2 $m
(20) да. Значение Ссылки нужной группы, которую предварительно получить/найти



